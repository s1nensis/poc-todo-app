import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  fab: {
    position: 'absolute',
    backgroundColor: '#000',
    margin: 10,
    right: 0,
    bottom: 0,
    borderColor: '#fff',
    borderWidth: 1,
  },
});
